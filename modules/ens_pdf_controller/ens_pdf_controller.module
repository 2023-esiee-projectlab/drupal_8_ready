<?php

/**
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 * @return array
 */
function ens_pdf_controller_theme($existing, $type, $theme, $path): array
{
  $items['ens'] = array(
      'article_list' =>array(
          'variable' => array(
              'item'=> array(),
              'title'=>array(),
          )
      )
  );

  return $items;
}

